node_modules
dist
site
*.log
.DS_Store

# Claude Code 本機個人設定(各機器自有的權限 allowlist,不該共用)
.claude/settings.local.json

# Netlify 本機連結狀態(內含本機絕對路徑,每台機不同)
.netlify/

# 本機備份檔(0813:曾被誤 commit,移出版控)
*.local-bak-*
