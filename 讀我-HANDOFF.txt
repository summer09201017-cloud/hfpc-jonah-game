============================================================
讀我 — HANDOFF(給 HFPC 那台 PC 的 AI / 開發者)
更新:2026-06-13(新增「總入口大廳」已上線 + 戰爭闖關系列開工:摩西舉手已可玩、紅海奔逃 WIP)
============================================================

現在有「三個獨立但相關」的專案(前兩個是 2026-06-11 就有的,大廳是 06-13 新開):

  1) 保羅大富翁   ……  hfpc-paul-game\
        GitHub: https://github.com/summer09201017-cloud/hfpc-paul-game
        擲骰移動桌遊(roll-and-move)。多條旅程(保羅1/2/3/4 + 約拿 + 出埃及 + 但以理)、
        每城聖經問答、機會/命運卡、真實地理棋盤、輕 RPG 層、3D 骰子、宣教接力。
        城市觸發即時闖關小遊戲(嵌約拿引擎)。React + Vite PWA。手機強制橫式全螢幕。
        ★ 已上線:https://hfpc-paul-game.netlify.app/(push main 即自動部署)

  2) 約拿闖關     ……  hfpc-jonah-game\   ← 本資料夾
        GitHub: https://github.com/summer09201017-cloud/hfpc-jonah-game
        即時 2D Canvas,「約拿書」全書六關。純 vanilla JS、零相依、零美術檔、
        Web Audio 合成、可離線 PWA。★ 已上線:https://hfpc-jonah-game.netlify.app/
        ★ 06-13 起也是「戰爭闖關」動作關的原型工坊(見下方,?level=moses)。

  3) 🆕 聖經闖關大富翁遊戲(總入口大廳)…… hfpc-bible-games\
        GitHub: https://github.com/summer09201017-cloud/hfpc-bible-games
        ★ 已上線:https://hfpc-bible-games.netlify.app/(連 GitHub,push main 自動部署,已實測)
        是什麼:一個「門口/啟動器」,卡片牆連到各遊戲。**本身不含任何遊戲**。
        純靜態、零相依、no-bundler(沿用約拿的避地雷慣例)、PWA(有「安裝到手機」按鈕)。
        加旅程只改一個檔 src/data.js(資料驅動),畫面碼不動。
        目前 5 張卡:約拿闖關(動作版,連約拿站)、約拿宣教之旅(桌遊版)、保羅、但以理、出埃及
        (後四張都用 ?journey= 連到保羅 app 內的旅程)。

■ 接手第一步:每個專案都先讀(在各自資料夾)
  1. CLAUDE.md   —— 架構與開發守則 + 這台機器的地雷(最重要)。
  2. roadmap.md  —— 「已完成 vs 真正待做」進度。
  3. README.md   —— 玩法與內容編輯(給老師)。
  (大廳 hfpc-bible-games 也有自己的 README.md。)

■ 現況一句話(2026-06-13)
  三個遊戲都已上線。大廳是新的唯一對外入口(以後發網址就發它)。
  「戰爭闖關」動作關系列剛驗證成立:第一關「摩西舉手」已可玩(?level=moses),
  牧師實測回饋已套(日落改 20 秒、操作簡化、加表情/兩軍標籤);
  第二關「紅海奔逃」設計稿+數值+文案已就緒,場景與繪製待寫(WIP,見下)。

============================================================
■ 🆕 戰爭闖關系列(2026-06-13 開工)—— 動作關,暫住約拿 repo 做原型
============================================================
  目標:做一系列「動作闖關」手感的聖經戰爭關(摩西舉手、紅海、大衛甩石…)。
  使用者已拍板:**要的是「動作闖關」手感**(不是卡片/決策關)。

  ▍架構決策(重要,別做錯):
    • 大廳永遠只放卡片,不裝遊戲。
    • 戰爭關「現在」暫住約拿 repo,只因為重用約拿引擎(storm 平衡 / spawner 跑酷)做原型最快。
    • **永久的家 = 驗證好玩後,搬進保羅 repo 當 in-repo 小遊戲**(消除跨 repo sync),
      因為戰爭關都屬於各書卷旅程(出埃及/撒上/士師),那些旅程住在保羅 app。
    • ⚠ 戰爭關是約拿 repo 的「未提交」工作,且**不該進約拿的公開遊戲**。
      所以:**別把這些改動 push 到約拿 main(會自動部署到線上約拿,把原型曝光給所有人)**。
      要版本控制就開 feature branch;對外轉移用壓縮檔(本包就是)。

  ▍第一關「摩西舉手之戰」(出 17:8–13)—— ✅ 已可玩(原型)
    • 試玩:npm run dev 後開 http://localhost:<port>/?level=moses(約拿 dev 常在 5174/5175)
    • 機制:平衡式(重用 storm.js 換皮)。armDrop=手的下垂。
      **操作已簡化(06-13 牧師實測「按左邊也輸」→ 原本兩區設計是陷阱,已廢除)**:
        按住畫面任意處 / 任意方向鍵 = 出力舉手;
        亞倫、戶珥在手垂下時「自動」上來扶(可用度有限會回充)。
      依靠值:你出力是主力,但後段疲勞 > 你獨力的 push → 必須有扶手;
        扶手獨力也不夠(supportPush < 後段 gravity)→ 光被動等扶手也會輸。兩者同工才贏。
    • 數值在 config.js 的 MOSES(duration 20s、push/supportPush/gravityGrow… 全可調)。
    • 文案在 scripture.js 的 MOSES(出 17、耶和華尼西、亞 4:6)。
    • 程式:src/moses.js(場景,構造同 Storm)、game.js level===7 接線
      (startMoses / win / lose / restart / next / 嵌入白名單含 7 / boot 派發)、
      renderer.js 的 _drawMoses + _moses + _helper + _mosesHud + _statBar
      (山頂摩西舉杖+白髮頭巾+隨疲勞變化的表情、亞倫藍頭帶/戶珥紅褐頭帶可分辨、
       谷中兩軍【🔵以色列 / 🔴亞瑪力,有標籤】、太陽=計時、三條 HUD)。
      main.js 的 ?level=moses 捷徑、sw.js CACHE→jonah-v4 預快取 /src/moses.js。
    • 驗證:npm test 95 綠;headless 模擬三向行為正確
      (不操作@~11s敗、一直按住=勝、一直按左也=勝<陷阱已解>)。
    • 設計稿:docs\戰爭關-摩西舉手-設計稿.md。
    • 還想調:玩過覺得太難/太易就改 config.MOSES(gravityGrow↑變難、duration↓變短、
      supportDrainTime/RechargeTime 調扶手稀缺度)。

  ▍第二關「紅海奔逃」(出 14)—— 🔧 WIP(設計+數值+文案已就緒,場景與繪製待寫)
    • 已完成:docs\戰爭關-紅海奔逃-設計稿.md、config.js 的 REDSEA、scripture.js 的 REDSEA。
    • 玩法(已定):自成一格場景,level===8。phase: stand(站住等候,海漸開,出14:13)→
      cross(過海床、輕點跳過礁石、後方戰車追兵 lead 動態,絆到變慢追兵逼近)→
      closing(海合攏淹追兵 14:28)→ 過關;追兵追上=失敗。重用 game.player 的跳躍。
    • 待寫(估 ~半天,照摩西的步驟做):
        1) src/redsea.js —— RedSea 場景 class(constructor(game)/reset()/step()),
           讀 config.REDSEA;phase 機械 + 追兵 lead + 障礙跳判定 + 站住計時閘門。
        2) game.js —— import RedSea + REDSEA;this.redsea=new RedSea(this);
           startRedSea();step() 加 if(level===8) this.redsea.step(dt);
           win/lose/restart/next 加 level 8 分支;嵌入白名單加 8;boot 派發 level 8。
        3) renderer.js —— _drawRedSea(分開的海牆、海床捲動、奔跑先知 _prophet、
           戰車追兵、對岸、HUD bar、phase 提示);draw() 加 if(level===8)。
        4) main.js —— ?level=redsea 捷徑;sw.js CACHE→jonah-v5 + 預快取 /src/redsea.js。
        5) smoke-test.mjs —— levels 已可加 'REDSEA'(目前已加,驗證文案)。
        6) 用同樣的「headless 行為模擬 + 假 canvas 渲染模擬」驗證(見摩西做法)。

  ▍戰爭動作關「成本由低到高」排序(使用者已認可方向;做完一關再放行下一關):
     摩西舉手(✅平衡換皮)→ 紅海奔逃(WIP,跑酷+追兵)→ 約拿單爬崖(跑酷改直向)→
     大衛甩石簡化版(拋物線+命中,~1–1.5週,新引擎肌肉)→ 參孫推柱(蓄力 mash)→
     基甸火把夜襲(依序 QTE)。【押後】完整大衛(自由瞄準)/約書亞日頭停住(時間系統)/
     耶利哥真節奏版(音訊同步,坑深)。

============================================================
■ 兩個遊戲怎麼關聯(很重要,沒變)
============================================================
  保羅是外框(桌遊);約拿是上游小遊戲引擎。保羅 src\minigames\jonah\ 是約拿引擎純複製,
  在保羅資料夾跑 npm run sync:jonah 一鍵同步。嵌入契約寫在約拿 CLAUDE.md(改引擎前必讀):
    • ui 由外部注入,game.js 不 import ui.js;opts.embed 走嵌入分支;
    • 嵌入白名單 level 1~7(7=摩西舉手,純 Canvas 關,配 NullUI 即可);
    • 1/2/4/7 純 Canvas 關配 NullUI;3/5/6 卡片流程關由保羅 EmbedUI React 卡片接手。

  ▍大廳 → 保羅的 deep-link(06-13 新加,⚠ 待保羅重新部署才在線上生效):
    保羅 SetupScreen.jsx 已加「讀網址 ?journey=<key> 預選旅程」(向後相容)。
    大廳卡片用 …/?journey=paul|jonah|exodus|daniel 直接跳該旅程。
    **這是保羅 repo 的未提交改動**:要在線上生效,需在 HFPC PC 重新部署保羅
    (push main 或 netlify deploy)。未部署前點卡片會落在保羅選單,手選即可(卡片仍可用)。

============================================================
■ 怎麼跑 / 怎麼測(起手式)
============================================================
   約拿(hfpc-jonah-game):
     npm install ; npm run dev      # Vite;常跳 5174/5175
     npm test                       # 煙霧測試(內容/語法/狀態機/嵌入契約,~2 秒)
     npm run test:offline           # 再加 build + PWA 離線就緒
     npm run build                  # ⚠ 不是 vite build!(逐檔複製到 site/,見 CLAUDE.md 地雷)
     試玩戰爭關:dev 後開 ?level=moses(紅海寫好後 ?level=redsea)
   大廳(hfpc-bible-games):
     npm run dev                    # 零相依 serve.mjs,自動找空埠(5180+)+開瀏覽器
     npm test / npm run test:offline ; npm run build   # 同約拿風格
     加旅程:只改 src/data.js 的 JOURNEYS 一筆;soon:true = 敬請期待(不可點)
   保羅(hfpc-paul-game):
     npm run dev ; npm run validate ; npm run test:selfplay ; npm run test:browser ; npm run sync:jonah

============================================================
■ 已完成 vs 真正待做(三專案合併視角,2026-06-13)
============================================================
✅ 已完成
  大廳:★已上線 https://hfpc-bible-games.netlify.app/、GitHub 連動自動部署(已實測 git build 綠)、
        卡片牆+分類(聖經/RPG/戰爭)、資料驅動、PWA 安裝按鈕、離線。
  約拿:六關全書完、PWA 全 shell 預快取、npm test 全綠、★已上線、
        戰爭關第一關「摩西舉手」原型可玩(?level=moses,牧師回饋已套)。
  保羅:★已上線、多條旅程(保羅1-4 + 約拿 + 出埃及 + 但以理)、真實地理、輕 RPG、
        3D 骰子、宣教接力、手機橫式全螢幕、約拿全六關嵌入。

🔜 真正待做(必要,依優先)
  1. 紅海奔逃 #2 收尾(WIP,~半天):寫 redsea.js + game 接線 + _drawRedSea + 試玩捷徑 + sw
     (步驟見上「戰爭闖關系列」)。寫完用模擬驗證、npm test 綠。
  2. 保羅重新部署一次:讓大廳卡片的 ?journey= deep-link 在線上生效。
  3. 摩西/紅海 真人實測手感微調(config.MOSES / config.REDSEA);好玩 → 搬進保羅當 in-repo 關。
  4. 真實手機離線實測(三個遊戲都裝一台、關網路跑一遍)。
  5. 神學/題庫審核收尾(journey2 與約拿之旅 06-10 前舊題卡)。
  6. (若戰爭關確定成系列)把摩西+紅海從約拿 repo 搬進保羅 repo,接成出埃及旅程的闖關站。

============================================================
■ 🚀 未來功能 / Skill / Tool 點子(按「CP 值高→低 × 開發時間短→長」排序)
   給 HFPC PC 的 AI:跟牧師確認想做哪個,再開 feature branch 動工。
============================================================

【第一梯隊:半天內、立刻有感】
 1. 紅海奔逃收尾(見待做 #1)—— 已有設計+數值+文案,只差場景與繪製。 ~半天 ★最高 CP(離完成最近)
 2. 全書通關證書畫面(約拿)— 打完六關出可截圖的「通關證書」(名字+日期+金句)。純 Canvas。 ~2-3 時
 3. 大廳「戰爭闖關」分類卡 — 戰爭關上線後,大廳 data.js 加一張 category:'war' 卡(資料驅動,改一筆)。 ~10 分
 4. 上課前一鍵檢查 .bat — 雙擊跑 npm test + 印「今天可上課 ✓」,給不會終端機的同工(.bat 純英文+CRLF)。 ~1 時
 5. 班級計分板(保羅)— 結束畫面加全班排行榜(localStorage)。 ~2-3 時
 6. 約拿題庫擴充到 3–4 章(現 22 題只 1–2 章;補後可重開第四關長者 NPC)。 ~2 時

【第二梯隊:1-2 天、明顯加值】
 7. 約拿單爬崖 #3 戰爭關(撒上 14)— 跑酷引擎改「直向攀崖+守軍突襲」。 ~1-1.5 天
 8. 老師模式/講解暫停(約拿)— 反思卡加「老師講解」鈕:放大經文、隱藏按鈕,投影用。 ~半天
 9. 經文朗讀(Web Speech API 中文 TTS)— 反思/禱告卡按一下唸出和合本,給不識字小小孩。 ~半天
 10. 大廳加「最近更新/敬請期待」區 + 安裝引導圖文 — 提升入口的吸引力。 ~半天

【第三梯隊:2 天以上、大型但高回報】
 11. 大衛甩石簡化版 #4 戰爭關(撒上 17)— 拋物線+命中判定(新引擎肌肉,做好後投擲關都重用)。 ~1-1.5 週
 12. 真實美術 PNG sprite 換 emoji/向量(renderer 已隔離 _prophet/_sailor/_moses)。 ~2-3 天+美術
 13. 下一卷書遊戲(但以理/出埃及六關)— 套 arcade-game-kit + bible-game-studio。 ~1 週
 14. 多人同樂(保羅,平板/手機房間制,需後端或 P2P)。 ~1 週+

【Skill / Tool-Calling 點子(讓 AI 自己更好用,跨專案)】
 15. war-level-kit skill — 把「戰爭動作關」做法(平衡式如摩西 / 跑酷追兵式如紅海 /
     拋物線式如大衛)整理成跨專案 skill:含「自成一格場景 + 嵌入契約 + headless 模擬驗證」
     的範本。讓下一關照表操課。 ~半天 ★高 CP(已有兩關活範例)
 16. quiz-authoring skill — 「幫我出某卷書 N 題」→ 依 bible-game-studio 慣例產題 +
     跑 game-content-validator 驗 answerIndex。 ~半天
 17. playtest-bot skill — 把 Playwright 全自動通關(保羅 browser-playtest + 約拿 e2e)
     整理成跨專案「自動玩一遍找 bug」skill。 ~半天
 18. hub-journey-add tool/skill — 「在大廳加一張旅程卡」自動改 data.js + 跑 npm test
     (資料驅動,很適合做成一鍵工具)。 ~2-3 時
 19. sermon-companion skill — 從關卡反思文案 + packer-theology 產主日學教案/講章(.docx)。 ~半天
 20. headless-sim 驗證範本(Tool 化)— 摩西用的「假 game/假 canvas 跑 step()/draw() 抓
     行為與執行期錯誤」很好用,值得做成可重用範本,讓每個新關卡都能離線自我驗證。 ~2-3 時

============================================================
■ 共用的 Skill(裝在 C:\Users\<帳號>\.claude\skills\)
============================================================
  引擎藍圖:arcade-game-kit(即時 Canvas)、roll-and-move-game(擲骰桌遊)、board-game-designer(棋類)、
            bible-game-scaffold(從保羅架構開新書卷專案)、bible-journey-planner(把一卷書變遊戲設計)
  內容/系列:bible-game-studio(聖經遊戲慣例)、bible-rpg-items(道具層)、packer-theology(巴刻神學)
  跨專案工具:embed-minigame(嵌入小遊戲)、add-challenge-station(棋盤加站)、
            game-content-validator(內容驗證)、game-smoke-test(上課前煙霧測試)、
            real-geography-board(真實地圖棋盤)、classroom-game-deploy(教室部署)、
            web-launch(一鍵啟動)、force-landscape-pwa(手機強制橫式)、
            pwa-install-button(安裝到手機按鈕,大廳已用)、game-art-upgrade(美術升級)

============================================================
■ 給 HFPC PC 的注意事項
============================================================
  • 三台路徑不同:本包解壓後路徑自帶;CLAUDE.md 裡的絕對路徑「因機器而異」。
  • agape250 那台 Node 24 地雷(vite build 會被無聲殺掉)是機器特性;三個專案的 npm run build
    都已改「逐檔複製到 site/」不打包,HFPC PC 照樣能用。
  • .bat 一律純英文 + CRLF。
  • ⚠ 戰爭關(moses/紅海)是約拿 repo 的未提交原型,**別 push 到約拿 main**(會自動部署上線曝光)。
    要版本控制請開 feature branch。
  • 大廳已在 GitHub + 自動部署;保羅的 deep-link 改動(SetupScreen.jsx)未提交,
    需在 HFPC PC 重新部署保羅才在線上生效。
  • git pull 各 repo 是拿最新最保險;本壓縮檔是備援 + 帶上「約拿 repo 的戰爭關未提交工作」。

============================================================
★ 2026-09-05 合併說明:以上是 feat/redsea(線上 v20,四個戰爭關)的交接;以下是 main(v7,曉臻人聲 / 分齡 / 打點修正)的交接。
  兩條線在 06-11 分岔、各自發展近三個月,0905 合回同一條(v21)並部署到 hfpc-jonah-game worker。
============================================================

讀我 — HANDOFF（給 HFPC 那台 PC 的 AI / 開發者）
更新：2026-06-15（晚）。接手請先讀這份整段，再進各 repo 讀 CLAUDE.md / README.md。
GitHub 帳號：summer09201017-cloud。GitHub 是唯一真相；桌面 zip 只是備援快照。
本檔已同步 commit 到 paul / bible-games / jonah 三個 repo 的 main。
============================================================

════════════════════════════════════════════════════════════
★ 最新對齊：2026-06-17（agape250 的 AI 先讀這段。完整現況/待做/CP 排序在 hfpc-paul-game）
════════════════════════════════════════════════════════════
  本輪（06-15 晚～06-17）主要在 hfpc-paul-game，全部已併 paul main、自動部署、線上可玩：
  • 挪亞兩關 + 大廳卡；arkpairs 難度旋鈕+星等+無黑邊。
  • 聖歌(約沙法)+反轉(巴蘭) 卡片關升級 L6 手繪 Canvas、去 emoji。
  • 盼望·以利亞動作關：遇天使後空中撒餅水、跳起來吃才回體力 + 台詞精簡。
  • 聖歌動作版「搬進 paul」（?demo=jehoshaphat-action）+ 敵軍放大~1.35×加眉毛嘴巴(三表情)。
  • 動作關手機全螢幕一條龍（8 關）：16:9 滿版、遊玩時隱藏標題、⛶+暫停鈕、開場框可捲、挖孔鋪底。

  ⚠ 本 repo（約拿）相關注意：
  • jonah main 仍是「約拿六關」正式站，**戰爭關（摩西/紅海/聖歌/巴蘭）只在 feat/redsea，別 push 到 main**。
  • 聖歌敵人「放大+眉毛嘴巴」改動已在 **feat/redsea**（已 push）；war-games 站要 `npm run build` → netlify CLI 手動重部署才生效。
  • 但「戰爭關永久家＝paul」：聖歌已搬進 paul（?demo=jehoshaphat-action）、大廳已改指向 paul，
    所以**聖歌新版敵人其實已隨 paul 自動上線**；war-games 只是舊原型站。巴蘭/摩西可比照搬進 paul。
  • 新跨專案 skill ×2：embed-fullscreen-fit（手機全螢幕嵌入固定比例 Canvas 關）、
    difficulty-knob（難度旋鈕 輕鬆/普通/挑戰 + ⭐星等，資料驅動、不會把「不會失敗」的關變成會輸）。

  鐵律：走 PR 不直推 main；大廳連 paul ?demo= 要「先部署 paul 再併大廳」否則 404；
        卡片關一律 L6 手繪、動作關遊玩時隱藏標題；零美術檔改完用 Playwright 手機橫向截圖驗收。
  → 完整「已完成 vs 待做」+ CP 排序：hfpc-paul-game 的 roadmap.md(最上方 2026-06-17 段) 與
    docs/未來功能與skill點子-CP排序.md。

■ 一句話現況（2026-06-15 晚）
  暑假兒童營主題「逆轉奇兵」五奇兵已全部上線（每個都有卡片版+動作版，大廳 10 格 5列2欄全亮）；
  紅海奔逃動作關大幅強化並上線；挪亞方舟兩關（另一 session 並行）已進 PR。今天大量收尾 + 上線。

■ 三＋一個站台（都在 summer09201017-cloud，Netlify 自動部署，除 war-games）
  ┌ repo / 站 ──────────────────┬ 線上 ───────────────────────────────────────────────┐
  │ hfpc-bible-games（大廳）     │ hfpc-bible-games.netlify.app。逆轉奇兵(10格)+戰爭闖關合輯。│
  │ hfpc-paul-game（React 主程式）│ hfpc-paul-game.netlify.app。七旅程 + 各 ?demo= 單關。      │
  │ hfpc-jonah-game（vanilla 引擎）│ hfpc-jonah-game.netlify.app（六關正式站）。               │
  │ hfpc-war-games（戰爭原型站）  │ hfpc-war-games.netlify.app。?level=moses/redsea/jehoshaphat/balaam │
  └─────────────────────────────┴──────────────────────────────────────────────────────────┘
  ⚠ war-games = 「約拿 feat/redsea build → netlify CLI 手動部署」(不連 GitHub 自動)。
    指令：在 hfpc-jonah-game 下 `npm run build` →
    `netlify deploy --prod --dir site --site 7addaadf-d3cd-4029-9a0b-7143b902fc98 --no-build`
    改約拿動作關 = 重 build 重 deploy + sw 版本 +1。目前線上 = sw v18。
    ✅ 這台(agape250)已裝 netlify CLI 且登入(DeYi/台北信友堂)；HFPC 那台若沒裝要先 `npm i -g netlify-cli` + `netlify login`。

■ 工作環境（重要！）
  - 真 git 工作樹：C:\Users\agape250\Downloads\hfpc-git\（三個 clone）。
    桌面的「0615…」等資料夾是解壓快照、沒有 .git，別在那邊改。
  - 這台沒裝 gh CLI，但 Git Credential Manager 已快取憑證 → `git push` 直接可用；
    開/合 PR 用 MCP github 工具（或網頁）。
  - Node 24：`vite build` 偶爾無聲死 → 先手動刪 dist/ 再 build；看 dist/index.html 時間戳確認。
  - 沒裝 jq（hook/腳本別依賴 jq）。

■ ★ 逆轉奇兵【五奇兵 × 兩形態 = 10 格】現況（全部已上線，大廳 5列2欄）
  ┌ 奇兵 ──┬ 卡片版（?demo=）─────────┬ 動作版（?demo= 或 war-games ?level=）─────┐
  │🕊️福音 │ ✅cornelius            │ ✅cornelius-action（跑酷,約帕→該撒利亞）  │
  │🌅盼望 │ ✅elijah（文案待審）   │ ✅elijah-action（撿餅水,天使有表情+遞餅水）│
  │💡大光 │ ✅saul（文案待審）     │ ✅saul-action（曠野跑酷,大馬士革路→直街）  │
  │🎵聖歌 │ ✅jehoshaphat（待審）  │ ✅war-games ?level=jehoshaphat（讚美得勝）│
  │🫏反轉 │ ✅balaam（待審）       │ ✅war-games ?level=balaam（閃避,長按加速,天使×0.9,有背景音樂）│
  └────────┴────────────────────────┴──────────────────────────────────────────┘
  ＋戰爭合輯🌊紅海：?demo=redsea（出14,獨立入口,不綁約拿/不併保羅旅程）。

■ 🌊 紅海奔逃 — 今天做到很完整（?demo=redsea + war-games ?level=redsea，都 live）
  站住等海開 → 跑過海床 → 海合攏淹追兵=過關。反向RPG：沒有攻擊鍵，唯一的路是神開的海路。
  已含：渡海時間×2(goalDistance 8400)、可加速衝刺(長按畫面任意處 或 →/D)、手機滿版全螢幕、
  更緊張(障礙密、追兵逼)、水中動物障礙(🦀螃蟹快速衝/🐍海蛇/🦂水蠍子，數量×0.8)、
  跳到動物身上=踩死(壓扁+💥,在空中即踩死、好踩)、追兵急促背景音樂、
  法老追兵(馬頭朝右尾左、士兵兇狠表情+頭盔、5輛追/closing 7輛)、過關海水從上下合攏把全軍沖走、人仰馬翻、淹沒(出14:28)。
  引擎源頭在 jonah feat/redsea；保羅端走 sync:jonah。校準：scripts/_redsea-tune.mjs 三閘門、_redsea-render-check.mjs。

■ 🛕 挪亞方舟（另一 session 並行做，PR #15，未合併）
  paul src/minigames/arkpairs/（一公一母翻牌配對 + 安排房間相鄰約束解謎）、arkbuild/（操作挪亞鎚擊放木板+嘲笑者，5段創6:14-22）。
  ?demo=arkpairs / ?demo=arkbuild；play-ark-pairs.bat / play-ark-build.bat。大廳已加挪亞合輯卡(在 bible-games 分支，待合併部署)。
  ⚠ 跨 session 教訓：未 commit 的源碼曾被 `git clean` 誤刪(已從 dist 救回)。一律做完即 commit+push；多 session 共用工作樹時各用 `git worktree`。

■ ✅ 今天已完成並上線（合併進 main、已部署）
  paul：#11 逆轉奇兵全套(2卡片關+紅海+福音/大光動作版+以利亞天使) / #12 紅海(×2+衝刺+手機滿版+更緊張) /
        #13 手機地圖放大空白修復(縮放上限依DPR動態夾) / #14 紅海水中動物 / #16 長按加速+踩死+紅海/巴蘭背景音樂 /
        #17 踩死更好踩+障礙×0.8+play-redsea.bat。
  bible-games：#2 逆轉奇兵10格5列2欄+紅海卡 / #3 大廳預設改暖米白(+5種換色外皮可切換)。
  war-games：部署到 sw v18（moses/jehoshaphat/balaam/redsea 全部最新；巴蘭天使×0.9慢×0.9少+長按加速+音樂）。

■ 🔴 真正待做（接手第一優先）
  1.【先做】牧者審核剩餘卡片版文案：盼望elijah、大光saul、聖歌jehoshaphat、反轉balaam（都已上線但文案是AI重用+草擬）。
     送審工具：paul `npm run`（export-quiz-review）或直接玩 ?demo= 逐句看。— 0.5 天（審）。CP 最高（營會前必做）。
  2. 合併兩個待合 PR 並實機驗收：
     - paul #18（法老追兵表情/馬頭朝右/數量/過關海水把全軍沖走+人仰馬翻+淹沒）— 已驗證，可直接合。
     - paul #15（挪亞兩關，另一 session）+ bible-games 的挪亞大廳卡分支 — 合併序：先 paul #15 部署，再合大廳。
  3. 手機實機回歸測試：地圖放大不再空白(RASTER_BUDGET=3200 可再調保守)、紅海手機滿版/長按加速/踩死、巴蘭。

■ 🚀 還能加的功能（CP 值高→低 × 開發時間短→長；給 HFPC 那台 AI 接著做）
  【半天內、立刻有感、CP 極高】
   A. 老師一鍵「今天可上課 ✓」.bat：跑 npm test + 印報告（綠燈才放心開課）。~1hr。
   B. 全奇兵「離線啟動選單頁」一張（本機 HTML，連各 ?demo=/?level=）——分站投影最實用。~2-3hr。
   C. 通關證書/獎狀畫面（名字+奇兵+金句，可截圖列印）。~2-3hr。
   D. 大廳「逆轉奇兵」每卡的 soon 都已拿掉；剩補大光/福音動作版的 bespoke 場景（目前通用跑酷）。~各半天。
  【1-2 天、明顯加值】
   E. 金句收集冊/圖鑑（答對收金句貼紙，localStorage）。~1天。
   F. 分組計分板自動進分（大廳已有 #/scoreboard，串接各關得分）。~半天-1天。
   G. 卡片關 L6 級手繪 Canvas 場景（取代 emoji，見 card-canvas-scenes skill）逐關升級。~各半天。
   H. 把 war-games 的 moses/jehoshaphat/balaam「搬進保羅」當 ?demo（像 elijah-action），擺脫 netlify CLI 依賴、跨機可自動部署。~1天。
  【大型、營會後再說】
   I. 挪亞接真棋盤站點 + 整卷旅程/題庫/道具（見 add-challenge-station / bible-journey-planner / bible-rpg-items）。
   J. 挪亞畢業成獨立 repo hfpc-noah-game（見 bible-game-scaffold 走法 B）。
   K. 真實美術 PNG（renderer/scenes 已隔離，可後換；先截圖比對再定案，見 game-art-upgrade）。
   L. 雲端存檔/班級排行榜（Supabase）。

■ 🧩 跨專案 Skill（已建立 ✅ / 建議再做）
  ✅ 今天新增並驗證：
     - reverse-rpg-design：得勝靠倚靠神、不靠攻擊力的反向RPG設計心法（紅海/摩西/約沙法/盼望…）。
     - theme-skins：任何網站/PWA 加「換色外皮」切換器（活範例：大廳5皮）。
     - arcade-juice：即時小遊戲「手感三件組」——長按加速 / 踩死敵人 / 每關不同程序化背景音樂（活範例：紅海/巴蘭）。
     - match-pairs-minigame（另一 session）：翻牌記憶配對 +（可選）相鄰約束安排解謎（活範例：arkpairs）。
  • 建議再做（CP 高→低）：
     - quiz-authoring：幫某卷書產 N 題 + 自動跑 validator + 產牧者送審清單。直擊最大瓶頸（審題）。
     - classroom-hub-lobby：大廳合輯卡 + 深連結 + soon 旗標 + 分站/輪流/離線守則（把 bible-games 慣例固化）。
     - bible-minigame-two-forms：一故事→卡片版+動作版雙形態，並在大廳並列（逆轉奇兵的做法固化）。
  • 可改進的現有 skill：
     - embed-minigame：補「動作關兩種家」war-games ?level（需 netlify CLI）vs 保羅 in-repo ?demo（推薦、自動部署）。
     - classroom-game-deploy：補 war-games 專屬 site id + --no-build 手動部署、沒裝 netlify CLI 的備援。

■ 🛠️ 建議的 Tool Calling / 自動化（給 HFPC 那台 AI）
  ✅ 已裝：危險指令防呆 hook（~/.claude/settings.json PreToolUse/Bash，攔 git clean -f / reset --hard / rm -rf → 要求先確認；
     用 grep 不用 jq；若沒生效開一次 /hooks 載入）。
  • 截圖驗收鏈：paul 已裝 Playwright，逐關開 ?demo=/?journey= 截圖 + 抓 pageerror（零美術檔=改完一定截圖）。
     約拿沒裝 Playwright，可用 scripts/_redsea-render-check.mjs（假 canvas 驅動真 Renderer）抓執行期錯誤。
  • 上線前檢查鏈：validate + selfplay + smoke + test:offline + 看 dist/index.html 時間戳（防本機 build 無聲死）。
  • 多 session 同工作樹：各開 `git worktree` 隔離；危險 git 動作前先 dry-run（git clean -nd）、用 git stash --include-untracked。
  • MCP：github（開/合 PR）、memory（跨對話記住決定）。寄信/寫表：skill send-email、gsheet-write（gated，寄前先給人看）。

■ ⚠ 地雷（接手務必守）
  1. GitHub 是唯一真相；一律在 hfpc-git 的 clone 工作；文件跟程式一起 commit；動手前先確認在哪個分支/有沒有別的 session。
  2. 併 main = Netlify 自動部署（paul/大廳/約拿正式站）。約拿正式站別放動作關原型（會曝光）；動作關走 war-games 或保羅 ?demo。
  3. 牧者把關：未審文案別當定稿；新產題/文案一律牧者審。
  4. .bat 純英文 ASCII + CRLF（.gitattributes 已鎖）；啟動 `npm run dev -- --open "/?demo=KEY"`；別寫 Unix 的 >/dev/null（要 nul）。
  5. 卡片 spec 的 answer/lives 不可越界；過 validate / npm test。
  6. Node 24 vite build 偶爾無聲死：先刪 dist 再 build。Netlify(Node 20)不受影響。
  7. 零美術檔：改完場景一定截圖（保羅 Playwright）或跑 headless 渲染檢查（約拿）驗收。
  8. 約拿 level：7 摩西、8 紅海、9 聖歌、10 反轉；feat/redsea 才有這些（刻意不併約拿 main）。
  9. 保羅 src/minigames/jonah/ 是 sync:jonah 的 verbatim 複製；改動作關手感改約拿端再 sync，別直接改保羅那份。
 10. 多 session：未 commit 的檔會被別人 `git clean` 清掉——做完即 commit+push；共用工作樹各用 worktree。

■ 怎麼跑 / 怎麼測
  保羅（React+Vite）：npm install → npm run dev(:5173)。測：npm test（validate+selfplay+smoke）、npm run build、npm run test:offline。
     單關：?demo=redsea / cornelius / cornelius-action / saul / saul-action / elijah / elijah-action / arkpairs / arkbuild / sling。
  約拿（vanilla）：npm install → npm run dev。動作關單獨玩：?level=moses/redsea/jehoshaphat/balaam。
     紅海驗證：node scripts/_redsea-tune.mjs、node scripts/_redsea-render-check.mjs。
  大廳：npm run dev。push main 自動部署。

■ 接手第一件事（建議順序）
  1. git clone/pull 三 repo（在 hfpc-git\）；git fetch --all 看待合 PR（paul #18 法老追兵、#15 挪亞；bible-games 挪亞大廳卡）。
  2. 讀本檔 → 找牧者審 4 個卡片版文案（待做 1）。
  3. 合併 paul #18（可直接）；合併 #15 挪亞並部署，再合大廳挪亞卡。
  4. 手機實機回歸（地圖縮放、紅海、巴蘭）。
  5. 把 claude-skills（reverse-rpg-design / theme-skins / arcade-juice / match-pairs-minigame）裝進 ~/.claude/skills\；危險指令 hook 在 ~/.claude/settings.json。
  6. 接著挑「還能加的功能」A→D（CP 高、半天內）做起。
============================================================
